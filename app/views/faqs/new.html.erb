<% breadcrumb :faq_add, @parent %>

<div class="section-header">
  <div class="section-title">
    <%= fa_icon "question-circle" %>
    <%= t(".title", parent: @parent) %>
  </div>
</div>

<%= render 'form', faq: @faq, parent: @parent , :locals => {:country => @parent.country} %>
