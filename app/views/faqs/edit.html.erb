<% breadcrumb :faq_edit, @faq %>

<div class="section-header">
  <div class="section-title">
    <%= t(".title", parent: @faq) %>
  </div>
</div>

<%= render 'form', faq: @faq, parent: nil, :locals => {:country => @parent.country} %>
