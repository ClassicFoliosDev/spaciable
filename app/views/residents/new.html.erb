<% breadcrumb :resident_add, @plot %>

<div class="section-header">
  <div class="section-title">
    <%= t(".title") %>
  </div>
</div>

<%= render 'form', resident: @resident, plot: @plot, :locals => {role: :homeowner} %>



