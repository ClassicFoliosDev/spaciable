Hello <%= @resource.first_name %>,

<% if RequestStore.store[:living_req].blank? %>

	Someone has requested a password reset. You can do this through the link below.

	<%= resident_edit_password_route(@resource, @token) %>

	Your password won't change until you access the link above and create a new one.

	If you didn't request this, please ignore this email.
<% elsif  RequestStore.store[:living_req].present? %>
    A password reset has been requested for your Spaciable Living account.

    If you requested this, you can create a new password using the link below.

	<%= resident_edit_password_route(@resource, @token) %>

    If you didn't request this, please ignore this email

    Kind Regards,

    The Spaciable living Team
<% end %>
	

