<% breadcrumb :finish_type_edit, @finish_type %>

<div class="section-header">
  <h3 class="section-title">
    <%= fa_icon "folder" %>
    <%= t(".title", name: @finish_type.name) %>
  </h3>
</div>

<%= render 'form', finish_type: @finish_type, finish_categories: @finish_categories %>
