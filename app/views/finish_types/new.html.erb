<% breadcrumb :finish_type_new %>

<div class="section-header">
  <div class="section-title">
    <%= t(".title") %>
  </div>
</div>

<%= render 'form', finish_type: @finish_type, finish_categories: @finish_categories %>
