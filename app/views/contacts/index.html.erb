<% breadcrumb :contacts, @parent %>

<%= render @parent %>

<%= render 'components/tabs', tabs: tabs_for(@parent, "contacts"), parent: @parent %>

<%= render_collection @contacts, parent: @parent, can_create: (can? :create, @contact) %>
