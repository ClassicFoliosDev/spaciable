<% breadcrumb :custom_tile_edit, @custom_tile %>

<div class="section-header">
  <div class="section-title">
    <%= t(".title", tile: @custom_tile.feature? ? t("activerecord.attributes.custom_tiles.features.#{@custom_tile.feature}") : @custom_tile.title) %>
  </div>
</div>

<%= render 'form', custom_tile: @custom_tile, parent: nil, tile_parent: @parent %>
