<% breadcrumb :phase_timeline_new, @phase %>

<div class="section-header">
  <div class="section-title">
    <%= t("phases.phase_timelines.#{@scope}.title") %>
  </div>
</div>

<%= render 'form', phase: @phase, phase_timeline: @phase_timeline, timelines: @timelines, plots: @plots, scope: @scope %>
