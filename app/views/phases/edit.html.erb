<% breadcrumb :phase_edit, @phase %>

<div class="section-header">
  <div class="section-title">
    <%= fa_icon "vcard" %>
    <%= t(".title", phase_name: @phase.name) %>
  </div>
</div>

<%= render 'form', phase: @phase, development: @development %>
