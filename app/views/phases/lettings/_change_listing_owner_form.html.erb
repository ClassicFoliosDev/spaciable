<div class="hidden <%= listing_id %>">
  <%= simple_form_for listing, url: phase_lettings_path(phase), remote: true do |f| %>
    <div>
      <h3><%= t(".are_you_sure",
        oldowner: listing.admin? ? "admin" : "homeowner",
        newowner: listing.admin? ? "homeowner" : "admin",
        plot: listing.plot_number) %></h3>
      </div>
  <% end %>
</div>
