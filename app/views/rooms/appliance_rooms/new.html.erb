<% breadcrumb :room_edit, @room %>

<div class="section-header add_appliance">
  <h3 class="section-title">
    <i class="fa fa-building"></i>
    <%= t(".title", room_name: @room.name) %><br>
  </h3>
</div>

<%= simple_form_for([@room, @appliance_room]) do |f| %>
  <% if @appliance_room.errors.any? %>
    <div class="row">
      <div class="row-item">
        <div class="submission-errors">
          <ul>
            <% @appliance_room.errors.full_messages.each do |message| %>
                <li>
                  <%= fa_icon "exclamation-circle" %>
                  <%= message %>
                </li>
            <% end %>
          </ul>
        </div>
      </div>
    </div>
  <% end %>

  <div class="row appliances">
    <div class="row-item third">
      <fieldset>
        <div class="appliance-category ui-front select-container">
          <%= select_tag "categories", options_from_collection_for_select(@appliance_categories, "id", "name") %>
        </div>
      </fieldset>
    </div>

    <div class="row-item third">
      <fieldset>
        <div class="appliance-manufacturer ui-front select-container">
          <%= select_tag "manufacturers" %>
        </div>
      </fieldset>
    </div>

    <div class="row-item third">
      <fieldset>
        <div class="appliance ui-front select-container">
          <%= select_tag "appliances", required: true %>
        </div>
      </fieldset>
    </div>
  </div>

  <div class="form-actions-footer-container">
    <div class="form-actions-footer">
      <%= link_to t(".back"), room_path(@room, active_tab: "appliances"), class: "btn" %>
      <%= f.submit t(".submit"), class: "btn-form-submit btn-primary" %>
    </div>
  </div>
<% end %>
