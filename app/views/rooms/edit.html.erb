<% breadcrumb :room_edit, @room %>

<div class="section-header">
  <h3 class="section-title">
    <%= fa_icon "vcard" %>
    <%= t(".title", room_name: @room.name) %>
  </h3>
</div>

<%= render 'form', room: @room %>
