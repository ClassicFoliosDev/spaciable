<% breadcrumb :room_new, @unit_type %>

<div class="section-header">
  <h3 class="section-title">
    <%= fa_icon "vcard" %>
    <% t(".add", unit_type_name: @unit_type) %>
  </h3>
</div>

<%= render 'form', room: @room, parent: @unit_type %>
