<head>
  <%= stylesheet_link_tag 'manifest_homeowner', media: 'all', 'data-turbolinks-track': 'reload' %>

  <%-# --- If there is a plot (if the homeowner is logged in), check whether the plot is expired --- -%>
  <%-# --- If expired load expired branding, otherwise load branded branding --- -%>
  <% if plot %>
    <% if plot.expired? %>
      <%= render "components/homeowner/expired_template" %>
    <% else %>
      <%= render "components/homeowner/brand_template", brand: brand if brand %>
    <% end %>
  <% else %>
    <%= render "components/homeowner/brand_template", brand: brand if brand %>
  <% end %>

  <%= render "layouts/common_header" %>

</head>
<!DOCTYPE html>
<!--[if IEMobile 7 ]> <html dir="ltr" lang="en-US"class="no-js iem7"> <![endif]-->
<!--[if lt IE 7 ]> <html dir="ltr" lang="en-US" class="no-js ie6 oldie"> <![endif]-->
<!--[if IE 7 ]>    <html dir="ltr" lang="en-US" class="no-js ie7 oldie"> <![endif]-->
<!--[if IE 8 ]>    <html dir="ltr" lang="en-US" class="no-js ie8 oldie"> <![endif]-->
<!--[if (gte IE 9)|(gt IEMobile 7)|!(IEMobile)|!(IE)]><!--><html dir="ltr" lang="en-US" class="no-js"><!--<![endif]-->
