<% breadcrumb :finish_edit, @finish %>

<div class="section-header">
  <div class="section-title">
    <%= fa_icon "shower" %>
    <%= t(".title", name: @finish.name) %>
  </div>
</div>

<%= render 'form', room: @room, finish: @finish %>
