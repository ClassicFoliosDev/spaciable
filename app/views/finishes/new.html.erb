<% breadcrumb :finish_new, @finish %>

<div class="section-header">
  <div class="section-title">
    <%= fa_icon "shower" %>
    <%= t(".title") %>
  </div>
</div>

<%= render 'form', finish: @finish %>
