<div class="section-header">
  <div class="section-title">
    <% if division.expired? && !(current_user.cf_admin?) %>
      <div class="expired"><span class="expired-text"><%= t(".expiry").html_safe %></span></div>
    <% end %>
    <div><%= division.division_name %></div>
    <div class="about">
      <br/>
      <span class="overview">
        <% if current_user.cf_admin? || division.cas %>
          <%= t("cas_enabled", status: division.cas ? t("enabled") : t("disabled")).html_safe %><br/><br/>
        <% end %>
        <strong><%= t(".active_plots") %></strong><%= division.active_plots_count %><br/>
        <strong><%= t(".completed_plots") %></strong><%= division.completed_plots_count %><br/>
        <strong><%= t(".expired_plots") %></strong><%= division.expired_plots_count %><br/><br/>
        <strong><%= t(".developments") %></strong><%= division.developments.count %><br/>
        <strong><%= t(".phases") %></strong><%= division.phases.count %>
      </span>
      <span class="admins">
        <strong><%= t("administered_by") %></strong><br/><br/>
        <% administering_admins(division).each do |a| %>
          <%= "#{a.first_name} #{a.last_name}" %>
          <span class="inactive"><%= " (not activated)" unless a.invitation_accepted_at %></span><br/>
        <% end %>
      </span>
      <% developer = division.developer %>
      <% if developer.account_manager_name? %>
        <span class="account-manager">
          <strong><%= t("developers.developer.account_manager_name") %></strong><%= developer.account_manager_name %><br/>
          <strong><%= t("developers.developer.account_manager_contact") %></strong><%= developer.account_manager_contact %><br/>
          <strong><%= t("developers.developer.account_manager_email") %></strong><%= developer.account_manager_email %><br/>
        </span>
      <% end %>
    </div>
  </div>

  <div class="section-data">
    <%= render 'address/address', address: division.address %><br/>
    <%= render 'components/contacts', contact: division %><br/>
    <%= edit_btn [:edit, division.parent, division], element: division %>
  </div>
</div>
