<% breadcrumb :brand_new, @parent %>

<div class="section-header">
  <div class="section-title">
    <%= t(".title", name: @parent) %>
  </div>
</div>

<%= render 'form', brand: @brand, parent: @parent %>
