<% breadcrumb :brand_edit, @brand %>

<div class="section-header">
  <div class="section-title">
    <%= t(".title", name: @brand) %>
  </div>
</div>

<%= render 'form', brand: @brand, parent: @parent %>
