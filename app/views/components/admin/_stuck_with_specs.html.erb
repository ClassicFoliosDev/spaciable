
<% if current_user.cf_admin? || cas %>
  <%= render "components/admin/popup", preference: :show_stuck_with_specs_dialog, title: t("stuck_with_specs.title"), help: [t("stuck_with_specs.help")], type: :button, all_users: true, popup_on_load: false, width: 500 %>
<% end %>
