<span>
  <% if contact.contact_number? %>
    <i class="fa fa-phone"></i>
    <%= contact.contact_number %>
  <% end %>
  <% if contact.email? %>
    <i class="fa fa-envelope"></i>
    <%= contact.email %>
  <% end %>
</span>