<% breadcrumb :default_faqs, @faq_type %>

<div class="default_faq">

  <%= render 'components/tabs', tabs: default_faq_tabs(@faq_type) %>

  <%= render "collection", collection: @faqs, type: @faq_type %>

  <div class="form-actions-footer-container">
    <div class="form-actions-footer">
      <%= link_to t(".back"), admin_settings_path, class: "btn" %>
    </div>
  </div>
</div>
