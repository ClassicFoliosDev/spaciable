<% breadcrumb :admin_user_edit, @user %>

<div class="section-header">
  <div class="section-title">
    <%= t(".title", user: @user) %>
  </div>
</div>

<%= render 'form', user: @user, selected_role: @user.role %>
