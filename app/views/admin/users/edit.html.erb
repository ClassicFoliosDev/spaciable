<% breadcrumb :admin_user_edit, @user %>

<div class="row">
  <div class="row-item">
    <div class="section-header">
      <h3 class="section-title">
        <%= fa_icon "vcard" %>
        <%= t(".title", user_email: @user.email) %>
      </h3>
    </div>
  </div>
</div>

<%= render 'form', user: @user %>
