<% breadcrumb :document_edit, @document %>

<div class="section-header">
  <div class="section-title">
    <%= fa_icon "file-pdf-o" %>
    <%= t(".title", name: @document.title) %>
  </div>
</div>

<%= render 'form', document: @document, parent: nil, redirect_path: @redirect_path %>
