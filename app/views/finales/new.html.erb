<% breadcrumb :finale_add, @timeline %>

<%= render "tasks/timeline", timeline: @timeline, editable: false %>
<%= render "form", timeline: @timeline, finale: @finale, task: @task,
insert_mode: @insert_mode,  insertion_task: @insertion_task%>
